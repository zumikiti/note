# Shell 系のめも

## alias の設定
```sh
$ alias
alias branch='git branch'
alias gco='git checkout'
alias gf='git fetch -f' # -f をすることでリモートで消されたブランチを一覧から削除する
alias lint='composer lint'
alias phpunit='vendor/bin/phpunit'
alias pull='git pull'
alias push='git push'
alias status='git status'
```